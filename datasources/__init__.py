# where: datasources/__init__.py
# what: Marks the datasources directory as a Python package to enable relative imports.
# why: Required so that helper modules (e.g., kintone_formatter) can be imported from the datasource runtime and tests.
